<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <script>
          //task 1 arrow function
    // let n = parseInt(prompt("enter your number : "));
    // let factorial = (number)=>{
    // let fact = 1;
    // for (let i = 1; i <= number; i++) {
    //  fact = fact * i;
    //     }
    // console.log(fact);
    // }

    // factorial(n);


    //task 2 arrow function
    // let primeOrNot = ()=>{
    //     let num = 7;
    //     let isPrime = true;
    //     if (num <= 1) isPrime = false;
    //     for (let i = 2; i < num; i++) {
    //      if (num % i === 0) {
    //       isPrime = false;
    //            break;
    //      }
    // }
    //     console.log(isPrime ? "Prime" : "Not Prime");
    //      }
    //    primeOrNot();
    //    primeOrNot();

    //task 3 arrow function
        //     let n = Number(prompt("enter the number:"));
        // let amstrong = (num)=>{
        //     let temp = num;
        //     let sum = 0;

        //     while (temp > 0) {
        //     let digit = temp % 10;
        //     sum += digit * digit * digit;
        //     temp = Math.floor(temp / 10);
        //     }

        //     console.log(sum === num ? "Armstrong" : "Not Armstrong");
        // }

        // amstrong(n)

       // task 4 arrow function
        // let n = Number(prompt("enter the number:"));
        //  let table = (num)=>{
        //         for (let i = 1; i <= 10; i++) {
        //         console.log(num + " * " + i + " = " + (i * num));
        //         }
        //     }

        //     table(n);

        //task 5 arrow function
            //  let n = parseInt(prompt("enter the number"));

            // let sum = (n)=>{
            //     let sum = 0;
            //     while (n > 0) {
            //     sum += n % 10;
            //     n = Math.floor(n / 10);
            //     }
            //     console.log("Sum of digits:", sum);
            // }

            // sum(n);
        
            //task6 arrow function
            let str = prompt("Enter the words to palindrom:")
            let palindrom = (str)=>{
                   let answer = str.split("").reverse().join("");
                   console.log(str === answer ? "palindrom":"not palindrom");
            }
           palindrom(str);
           palindrom("MALAYALAM");
           palindrom("PRADEEP");
    </script>
</body>
</html>
